services:
  logbookviso:
    container_name: logbookviso
    build: .
    ports:
      - "5000:5000"
    environment:
      SYSTEM_DB: /data/system.sqlite
      PHOTOS_DIR: /data/photos
      LOGBOOK_UPLOAD_DIR: /data/logbook_uploads
      MAX_UPLOAD_MB: "256"
      # SECRET_KEY: set-a-long-random-string
      # INIT_ADMIN_USER / INIT_ADMIN_PASSWORD: nur beim ersten Start (leere User-Tabelle)
    volumes:
      - ./data:/data
    networks:
      - swag_proxy-net
    restart: unless-stopped

networks:
  swag_proxy-net:
    external: true
