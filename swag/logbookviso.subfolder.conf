## Version 2025/07/18
# SWAG Subfolder-Proxy für Logbook Viso
# Zugriff: https://deinedomain.tld/logbookviso/
#
# Hinweis: Subdomain-Proxy (logbookviso.subdomain.conf) wird empfohlen.
#
# 1. Container muss `logbookviso` heißen
# 2. Datei nach SWAG kopieren:
#      /config/nginx/proxy-confs/logbookviso.subfolder.conf
# 3. Gemeinsames Docker-Netz mit SWAG
# 4. SWAG neu starten

location /logbookviso {
    return 301 /logbookviso/;
}

location ^~ /logbookviso/ {
    # enable the next two lines for http auth
    #auth_basic "Restricted";
    #auth_basic_user_file /config/nginx/.htpasswd;

    # enable for ldap auth (requires ldap-location.conf in the server block)
    #include /config/nginx/ldap-location.conf;

    # enable for Authelia (requires authelia-location.conf in the server block)
    #include /config/nginx/authelia-location.conf;

    # enable for Authentik (requires authentik-location.conf in the server block)
    #include /config/nginx/authentik-location.conf;

    # enable for Tinyauth (requires tinyauth-location.conf in the server block)
    #include /config/nginx/tinyauth-location.conf;

    # Nginx-Default ist 1m → ohne diese Zeile: 413 bei Foto-Uploads
    client_max_body_size 512M;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
    proxy_request_buffering off;

    include /config/nginx/proxy.conf;
    include /config/nginx/resolver.conf;
    set $upstream_app logbookviso;
    set $upstream_port 5000;
    set $upstream_proto http;
    rewrite ^/logbookviso/?(.*)$ /$1 break;
    proxy_pass $upstream_proto://$upstream_app:$upstream_port;
}
